# vim: set sw=4 et:
#

language: python
python:
    - "2.7"
    - "3.2"
    - "3.3"

before_install:
    - sudo apt-get update
    - sudo apt-get -y install python-gdbm python3-gdbm python3.3-gdbm

install:
    - "pip install ."

before_install:
    - "pip install 'requests>=2.0.1' py.test"

script: py.test

